using System;
using System.Text;
using System.Collections.Generic;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Accord.Math.Integration;

namespace Alfheim.Tests.FuzzyLogicLifecycle
{
    /// <summary>
    /// Сводное описание для IntegrationTest
    /// </summary>
    [TestClass]
    public class IntegrationTest
    {
        public IntegrationTest()
        {
            //
            // TODO: добавьте здесь логику конструктора
            //
        }


        [TestMethod]
        public void IntegralTest()
        {
            //Func<double, double> f = (x) => x * Math.Cos(x);

            //// We would like to know its integral from -1 to +1
            //double a = 0, b = 2;

            //// Integrate!
            //double trapez = TrapezoidalRule.Integrate(f, a, b, steps: 1000);

            //Assert.AreEqual(Math.Round(trapez, 3), Math.Round(0,, 3));
        }
    }
}
